{{#if fields}}
<div class="attachment_fields">
  <table cellpadding="0" cellspacing="0" border="0" align="left">
    <tbody>
      <tr>
        {{#each fields}}
          {{> attachment_field}}
        {{/each}}
      </tr>
    </tbody>
  </table>
</div>
{{/if}}
